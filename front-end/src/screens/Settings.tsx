import { VStack, Heading, Center } from "native-base";

export function Settings() {
  return (
    <VStack flex={1}>
      <Center>
        <Heading my={24}>Settings</Heading>
      </Center>
    </VStack>
  );
}
